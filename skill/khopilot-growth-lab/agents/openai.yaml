interface:
  display_name: "Khopilot Growth Lab"
  short_description: "Topic-agnostic short-form virality engine (Khopilot default tenant)"
  brand_color: "#ff5a1f"
  default_prompt: "Use $khopilot-growth-lab to design a watchable short-form video: generate scored hooks, fill the beat×channel matrix, and produce the Flow prompt, AI Studio voiceover spec, and metric plan."

policy:
  allow_implicit_invocation: true
